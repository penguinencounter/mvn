[project]
name = "mvn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=15.0.0",
]

[dependency-groups]
dev = [
    "black>=26.5.1",
]

[tool.black]
target-version = ["py312"]
